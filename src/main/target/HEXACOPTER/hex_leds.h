
#pragma once

#include "drivers/io.h"

#define HEX_LED_TOGGLE(t)	IOToggle(IOGetByTag(IO_TAG(t)))
#define HEX_LED_ON(t)		IOHi(IOGetByTag(IO_TAG(t)))
#define HEX_LED_OFF(t)		IOLo(IOGetByTag(IO_TAG(t)))

#define HEX_LED1_TOGGLE		HEX_LED_TOGGLE(PC0)
#define HEX_LED1_ON		HEX_LED_ON(PC0)
#define HEX_LED1_OFF		HEX_LED_OFF(PC0)

#define HEX_LED2_TOGGLE		HEX_LED_TOGGLE(PC1)
#define HEX_LED2_ON		HEX_LED_ON(PC1)
#define HEX_LED2_OFF		HEX_LED_OFF(PC1)

#define HEX_LED3_TOGGLE		HEX_LED_TOGGLE(PC2)
#define HEX_LED3_ON		HEX_LED_ON(PC2)
#define HEX_LED3_OFF		HEX_LED_OFF(PC2)

#define HEX_LED4_TOGGLE		HEX_LED_TOGGLE(PC3)
#define HEX_LED4_ON		HEX_LED_ON(PC3)
#define HEX_LED4_OFF		HEX_LED_OFF(PC3)

#define HEX_LED5_TOGGLE		HEX_LED_TOGGLE(PC4)
#define HEX_LED5_ON		HEX_LED_ON(PC4)
#define HEX_LED5_OFF		HEX_LED_OFF(PC4)

#define HEX_LED6_TOGGLE		HEX_LED_TOGGLE(PC5)
#define HEX_LED6_ON		HEX_LED_ON(PC5)
#define HEX_LED6_OFF		HEX_LED_OFF(PC5)
